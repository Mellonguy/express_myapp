<!DOCTYPE html> 
<html> 
  <head> 
    <title><%= title %></title> 
    <link rel="stylesheet" type="text/css" href="/stylesheets/tui-grid.css" />
    <script src="/javascripts/tui-grid.js"></script>
    <%
    const grid = new tui.Grid({
        el: document.getElementById('sample'),
        domWidth : 100,
        data: sampledata,
        columns: [
            {
                header: 'bodyserail',
                name: 'bodyserail'
            },
            {
                header: 'prodcd',
                name: 'prodcd'
            },
            {
                header: 'prodnm',
                name: 'prodnm'
            }
          ]
      });

      
    </script>
    
  </head> 
  <body> 
    <h1><%= title %></h1> 
    <p>Welcome to <%= title %></p>    
    <div id="grid"></div>
      <!-- <% if (data != null) { %> 
      <table border="1"> 
        <% for(var i=0; i < data.length; i++){ %> 
          <input type="hidden" name="prodparamlistJson" value="<%=JSON.stringify(data[i].prodparamlist)%>" /> 
          <tr> 
            <td width="200px"><%= data[i].prodcd %></td> 
            <td width="200px"><%= data[i].prodnm %></td> 
            <td width="300px"><%= data[i].prodparamlist %></td> 
          </tr> 
        <% } %> 
      </table> 
    <% } %>  -->
  </body> 


  
</html>
